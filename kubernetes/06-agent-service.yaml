apiVersion: v1
kind: Service
metadata:
  name: gcp-agent-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: gcp-agent
  ports:
  - name: http
    port: 80
    targetPort: 8001
    protocol: TCP

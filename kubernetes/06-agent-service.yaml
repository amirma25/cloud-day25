apiVersion: v1
kind: Service
metadata:
  name: adk-agent-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: adk-agent
  ports:
  - name: http
    port: 80
    targetPort: 8001
    protocol: TCP
